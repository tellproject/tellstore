set(_comp_file "${CMAKE_CURRENT_LIST_DIR}/TellStoreClientTargets.cmake")
if(TARGET tellstore-client)
    set(TellStore_FOUND True)
elseif(EXISTS ${_comp_file})
    include(${_comp_file})
    set(TellStore_FOUND True)
else()
    set(TellStore_FOUND False)
endif()
